<nav class="navbar">
    <div class="navbar-container container">
        <div class="navbar-brand">
            <NavLink href="" class="nav-logo">
                <span class="logo-text">Utkeern</span>
            </NavLink>
        </div>

        <button class="navbar-toggle" @onclick="ToggleMenu" aria-label="Toggle navigation menu" aria-expanded="@isMenuOpen">
            <span class="toggle-line"></span>
            <span class="toggle-line"></span>
            <span class="toggle-line"></span>
        </button>

        <ul class="navbar-menu @(isMenuOpen ? "open" : "")">
            <li class="nav-item">
                <NavLink href="" class="nav-link" @onclick="CloseMenu">Home</NavLink>
            </li>
            <li class="nav-item">
                <NavLink href="gallery" class="nav-link" @onclick="CloseMenu">Gallery</NavLink>
            </li>
            <li class="nav-item">
                <NavLink href="projects" class="nav-link" @onclick="CloseMenu">Projects</NavLink>
            </li>
            <li class="nav-item">
                <NavLink href="research" class="nav-link" @onclick="CloseMenu">Research</NavLink>
            </li>
            <li class="nav-item">
                <NavLink href="students" class="nav-link" @onclick="CloseMenu">Students</NavLink>
            </li>
            <li class="nav-item">
                <NavLink href="about" class="nav-link" @onclick="CloseMenu">About</NavLink>
            </li>
            <li class="nav-item">
                <NavLink href="tools/breadcrumb-generator" class="nav-link" @onclick="CloseMenu">Tools</NavLink>
            </li>
        </ul>
    </div>
</nav>

@code {
    private bool isMenuOpen = false;

    private void ToggleMenu()
    {
        isMenuOpen = !isMenuOpen;
    }

    private void CloseMenu()
    {
        isMenuOpen = false;
    }
}
